name: workflow-call

on:
  workflow_dispatch:
    inputs:
      testbatch:
        required: true
        type: string      

jobs:
  innerloop:
    runs-on: windows-latest
    strategy:
      matrix: 
        testsets: [set1,set2]

    steps:
    - name: print array      
      run: echo Run ${{ matrix.testsets }}

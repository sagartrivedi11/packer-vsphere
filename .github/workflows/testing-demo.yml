name: workflow-call

on:
  push:
    branches: [ "testing" ]
  pull_request:
    branches: [ "testing" ]
  workflow_call:
  workflow_dispatch:
    inputs:
      testbatch:
        required: true
        type: string      

jobs:
  innerloop:
    runs-on: [ self-hosted ]
    strategy:
      matrix: 
        testsets: [set1,set2]

    steps:
    - name: print array      
      run: echo Run ${{ matrix.testsets }}

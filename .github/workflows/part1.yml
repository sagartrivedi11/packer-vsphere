name: Part1
on:
  workflow_dispatch:
  workflow_call:    
  

env:
 variable1: 'test'


jobs:
  create:
    runs-on: [windows-latest]      
    steps:  
      - name: step1
        run: echo "First Job"    
  
  build:
   runs-on: [windows-latest]
   steps:
     - name: Step1
       uses: sagartrivedi11/rest/actions//part2@v1.0

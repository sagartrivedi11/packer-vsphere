name: Workflow
on:
  workflow_dispatch:

jobs:
  job-1:
    runs-on: [windows-latest]
    steps:     
      - name: Start Test         
        run: |
          Write-Host "Test"

  job-2:
    runs-on: [windows-latest]
    needs: [job-1]
    steps:
      - name: Step1   
        id: clone
        timeout-minutes: 1
        run: sleep 10

      - name: Step2
        if: ${{ failure() }}
        run: Write-Host "Retry"

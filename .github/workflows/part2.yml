name: Part2
on:
  workflow_call:
  workflow_dispatch:

jobs:
  job-1:
    runs-on: [windows-latest]
    steps:     
      - name: Start Test         
        run: |
          Write-Host "Test"

  job-2:
    runs-on: [windows-latest]
    needs: [job-1]
    steps:
      - name: Step1   
        id: clone
        timeout-minutes: 1
        run: sleep 65

  job-3:
    uses: sagartrivedi11/packer-vsphere/.github/workflows/part3.yml@main

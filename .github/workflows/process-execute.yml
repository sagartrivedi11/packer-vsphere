name: workflow-call

on:
  push:
    branches: [ "testing" ]
  pull_request:
    branches: [ "testing" ]
  workflow_dispatch:
    
jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: checkout
        uses: "actions/checkout@v3"
      - name: print array      
        run: echo 'Running'

name: Test Get-IP

on:
  workflow_dispatch:   
jobs:
  job1:
    runs-on: [windows-latest]  
    
    steps:
      - uses: actions/checkout@v3      

      - name: Create a matrix for test execution
        id: test-matrix
        run: |
          echo "Hello"
          exit 2

        
